name: center_for_biblical_studies
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  flutter_dotenv: ^5.1.0
  dio: ^5.7.0
  dartx: ^1.2.0
  get_storage: ^2.1.1
  recase: ^4.1.0
  logger: ^2.4.0
  jwt_decoder: ^2.0.1
  google_fonts: ^6.2.1
  syncfusion_flutter_calendar: ^27.1.48
  provider: ^6.1.2
  intl: ^0.19.0
  flutter_event_calendar: ^1.0.0
  shared_preferences: ^2.3.2
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  go_router: ^14.2.7
  equatable: ^2.0.5
  file_picker: ^8.1.2
  another_flushbar: ^1.12.30
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.12
  freezed: ^2.5.7
  json_serializable: ^6.8.0 # Generate JSON parsers: flutter pub run build_runner build --delete-conflicting-outputs

flutter:

  uses-material-design: true

  assets:
    - assets/images/
   # - assets/.env