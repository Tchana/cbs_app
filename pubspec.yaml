name: center_for_biblical_studies
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_dotenv: ^6.0.0
  dio: ^5.8.0+1
  dartx: ^1.2.0
  get_storage: ^2.1.1
  recase: ^4.1.0
  logger: ^2.5.0
  jwt_decoder: ^2.0.1
  google_fonts: ^8.0.0
  shared_preferences: ^2.5.2
  freezed_annotation: ^3.1.0
  url_launcher: ^6.3.1
  json_annotation: ^4.9.0
  flutter_svg: ^2.0.17
  get: ^4.7.2
  pdfx: ^2.8.0
  intl: ^0.20.2
  supabase_flutter: ^2.8.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.15
  freezed: ^3.2.4
  json_serializable: ^6.9.4 # Generate JSON parsers: flutter pub run build_runner build --delete-conflicting-outputs

flutter:

  uses-material-design: true

  assets:
    - assets/images/
   # - assets/.env